# Private native and cancellation acceptance

This is scoped live evidence, not full v0 completion. The selected fresh company
is `a4013353-a84d-49a1-8d2b-10a1caf896fe`; preserved external employees and the
old `a5ed` checkout were not changed.

## Latest native evidence —19:32 UTC

This checkpoint supersedes the earlier pending native-send/cancellation gates.
On bundle SHA `e1b7d7c…`, the actual composer sent `hermes-sdk-native-20260905`,
and its thread displayed Ada's complete81-word response. Run
`2af44fcc-f737-44df-bed9-7fa3c9e03252` committed726 identical UTF-8 bytes to the
Office message and one acknowledged memory write. Delivery proof is retained in
`/private/tmp/ortak-private-20260905/provisioning/hermes-sdk-native-20260905`.

The actual Cancel run button submitted a signed request for running
`351891f1-407b-4d4b-8d76-1ee0fa2713b9`. A running7054 image container was observed
before clicking; the UI changed from request pending to cancelled/acknowledged.
The request at19:31:48.606UTC was acknowledged at19:31:48.998UTC on attempt1;
successful exact-name Docker inventory confirmed absence, Office and memory
output counts were0. Evidence is under
`/private/tmp/ortak-private-20260905/provisioning/native-cancel-sdk-20260905`.

Those tests used the diagnostic Hermes worker7054/controller1863 pair. The
successful long response does not establish that earlier intermittent provider
timeouts are resolved. Broader Work/management/memory/semantic and fresh-install
acceptance remain open. A later schema66 native package `0f2ce6b3…` is built but
not yet launched; old PID12516 was quit normally and its bundle retained.

The sections below preserve prior artifact-specific observations.

## Source-built native application

The private Tauri build from the `14b1` checkout completed successfully in
session9011. Its protected frontend builds and TypeScript check passed; native
compilation took3m08s. The unsigned bundle is
`desktop/src-tauri/target/ortak-private-native/debug/bundle/macos/Ortak Private.app`,
identifier `dev.ortak.private20260905`. Executable SHA256:
`faf884ed9ba9714ec6bb1dcbcb9ceae8f431d9cdcdd8794aa6dd4bff492eeba5`.
The private identity harness passed8 tests, with1 unrelated compiled-flag case
ignored. Receipt: `/private/tmp/ortak-v0-evidence/native-build-20260905.json`.

The previous private app was quit through its normal UI. The exact new bundle
was launched through `private_native_services.py desktop` with the freshly
prepared owner environment, PID95017/session82138. Its public startup identity
matched `6e28673ad50250894e63ebbe83f3fe6e51ca954b51f449023ef4b9f6f2007867`.
No private key was entered, revealed, exported or copied through the UI.

Actual native accessibility and screenshot inspection verified:

- Manual connection to `ws://localhost:3038`, with the fresh owner's profile
  named `ortak-owner`; the private channel contains that owner and Ada.
- The original Ada-signed reply appears in Inbox and its Office thread:
  “17 ile 25’in toplamı 42’dir.”
- Employees shows Ada ACTIVE; Activity shows the real completed run, ordered
  queued/started/assistant/delivery-intent/completed events and live connection.
- The independent Office delivery receipt and memory acknowledgement are
  visible. Expanding memory shows the exact reply and source
  `office:b423b20bfcb5333ca73e184ccf29b7af8492e195f21fe7e7e70897fab21b3d92`.
- Reload timeline preserves one ordered event sequence and delivery/memory
  state. This is native UI evidence in addition to the separate signed SSE
  acceptance, not a mock bridge or browser preview.

First-launch usability gaps remain: a configured environment identity receives
misleading key-creation copy; the explicit compiled default relay still excludes
loopback auto-connect; community onboarding shows inherited Buzz starter-team
copy although private bootstrap correctly creates no starter agents. These must
be corrected before calling the private onboarding flow finished.

## Actual cancellation and containment

Run `93c867dc-cf39-4aa7-9117-c45000069fcd` originated from fresh human event
`074b1a182ce257f2d007a24681a474095f2add2983b1ea4709d58f0605ad1d28`.
Before cancellation, exact Docker inspection proved a running contained worker
`57d577e75812397413627fd2509ec685b3bd723ead2ed843d7287b7d6e494982`
using reviewed image
`sha256:f18aa9349e1821ddc8853f33d4d3a73eddd7636a4cea9467d5811b760b5777e1`.

The production signed client submitted one human cancellation at17:10:42.262UTC.
Request `91e10e01-c4e5-4f88-aeee-5dd3c98f4852` and the runtime stop record were
acknowledged on attempt1 at17:10:42.893UTC. The database run and controller's
read-only SQLite journal both report `cancelled`. A successful exact-name
Docker inventory proves the contained worker is absent. The ordered sequence
is queued→started→cancelled; Office output and memory-write counts are both0.
The provider itself is not claimed to have completed a response.

Evidence directory:
`/private/tmp/ortak-private-20260905/provisioning/cancel-loop-1b2053ffaab44f68b41bc0a2dff2daa1`.
It retains intent, submission, live-container proof, signed-request receipt and
terminal receipt. Signed operation receipt:
`/private/tmp/ortak-v0-evidence/signed-api-acceptance-817b24760fab43dfb95e59de3e6548fe/operator-80fff60c-2aea-4519-8b83-5ab3d8b1a3d4/receipt.json`.

Earlier attempts are retained and not counted as successful cancellation:

- Runs `90b4fe13-93fe-4cf8-a702-a24c25bbcaa7` and
  `857c0218-9138-4221-866f-db35dd491dd2` failed `provider_failed` before native
  UI cancellation could be submitted. The selected bridge's closed catch-all
  does not prove whether the cause was incomplete or oversized model output.
- The first container-proof harness omitted the `ortak-run:` start-key prefix;
  its later signed request for `19c00a5d-1c60-4e7c-8e38-82fdda03becf` correctly
  refused the already failed run. No cancellation POST was sent.
- The next harness inspected before Docker had created its child. A later
  request for `9ae9acfb-1fa2-44f0-ab1c-5bb32ae91879` raced provider failure;
  both stop receipts acknowledged containment, but terminal state remained
  failed. This is not the successful cancelled-state case above.

The native cancellation button was observed, but a successful native button
submission remains a separate acceptance item. Closed diagnostic categories are
being added to the next bridge artifact; they cannot retroactively explain the
old image's failures. Broad Work, provisioning management, memory/semantic,
recovery and final full-v0 gates remain open.

## Subsequent source and runtime checkpoint

The native cancelled-state card was also observed with cancellation acknowledged,
ordered queued/started/cancelled events, no scheduled memory write and live
Activity connected. This remains a native read, not a native cancellation POST.
Configured-identity onboarding, explicitly bound local auto-connect and private
Office copy now have passing source regressions; the current native executable
predates those fixes.

A real native composer attempt selected Ada from mention autocomplete. Send
refused locally with the inherited agent authorization error, leaving the draft
in place and no message published. Marker `hermes-e2-native-20260905` identifies
that retained intent. The private central-routing mention fix must pass its
production-seam tests and rebuilt native flow before this item can close.

The new pinned worker `sha256:baf828b237502da6bfdde3cd598d32b4f4f87979adbc64ee6d3fe0b548b9d79c`
and controller `sha256:090758781ef2ed301556d89dbb6f13394dbe310d13e52328f5194fe3a73520f0`
passed constructor, Office run-loop, Codex OAuth fixtures, Work full-text/silent
loop, HTTP recovery12 and real containment8. Evidence directories:
`/private/tmp/ortak-v0-evidence/hermes-e2-checks-a873cd8065d847bdaa71349dbad06ead`
and `/private/tmp/ortak-v0-evidence/hermes-e2-containment-93ce272df8554b8396735a3cfa57137b`.
Provider calls in these checks were0. Root subsequently deployed those exact
images, preserving the stopped old controller and a verified stopped-journal
backup of8 runs/22 events/2 probes. New controller ID:
`e14fdf36967383fda203513364f30210cea7d8d6f10fcde440dcb697908acada`.
Worker PID6260/session28241 resumed. The new diagnostics have not yet explained
the old provider failures; a new actual provider response is still pending.
