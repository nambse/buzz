# Reviewed G recovery support through schema73

## Latest source selection: native refresh, main still73

Root replaced stopped native49705/ac9 with native70131/session14591 at
`Sun Sep 6 04:30:24 2026`, SHA
`c524d2bc049e416e1b89e7e1a7bd39ab82fcfb417953aab8ddefe26dda5e4845`.
The build and exact launcher/owner receipts are in
`/private/tmp/ortak-v0-evidence/native-refresh74-build-352f5cc6a50b42d38f8778833b6322fa/`.
The source native observer now requires this owner, its full launcher SHA,
binary inode, CWD and original process start. Main schema, backend73 owners,
Honcho D2c and existing Hermes images remain the selection below. The build
directory's74 name does not assert a live schema74 migration.

The native receipt seam passed30 focused selection/prepare/native/tail unit
tests. The earlier122-test and actual read-only process evidence below remains
attributed to the ac9 generation; no new capture/preparation or installed tail
fixture was run for this metadata change. Schema74 database-only obligations
are described in `RECOVERY_SCHEMA74_DATABASE_OBLIGATIONS.md`; the selected live
`MAIN_SCHEMA_VERSION` stays73. Historical G4 frozen20 operators are untouched.

## Prior73 rollout selection and source observer evidence

Root completed the actual69→73 promotion in
`/private/tmp/ortak-private-20260905/rollouts/schema73-9cde353c5a0c464982ecffb99de1f671`.
Its `main-migrated.json`, `honcho-verified.json`, `backend-verified.json` and
`native-verified.json` record actual migration, preserved old rows and current
native signed readback. The current source inventory selects backend73 artifact
`backend73-1f7662869f514ec9b6c786e0b2ddae71`, Honcho D2c full ID
`33d4e53fd9443e6d167d465f934f3bd9a7e209bb8c80f16ce19e65774b135ff8` /
image `9358bd04cd45bf654a198313e05a682300d75dc645f780e85df5d5b29f367ede`,
and native ac9cea2d0dc74bec3bd767f8908ff6cbba5eb3a8a2cc10992acc73d491765e73.
Hermes controller9335/dbc9 and worker8ee stay unchanged. C2 and the semantic
listener remain outside this deployed selection.

The source observer now reads the actual nested73 ownership receipts. Relay
47740/session81272, API47806/46520 and management47852/95281 come from
`backend-verified.json`. The original worker47868 is stopped; only
`reviewed-opt-in/enabled-verified.json` selects worker51563/session31205 and its
full-ID final launcher. Native49705/session57116 comes from `native-verified.json`.
Each next observation still rechecks loaded inode/hash/start/CWD; saved sessions
alone never authorize a signal. Registration rebases only the exact reviewed
helper import literal, with historical launcher hashes preserved.

The exact current `config/api73.json` and `config/worker73-reviewed-opt-in.json`
hashes are required. The latter opts in only project
`3a06f7cf-ff9c-4deb-bb8c-7ef422eb9b6e`; its historical target epoch0 receipt does
not assert current fact eligibility or drain. A second employee, changed
configuration, scorer or reader cannot silently join this inventory. The local
read-only selection check passed31 public files and14 secret metadata entries;
no secret values, live store contents, provider, snapshot or preparation were
accessed/created by that check. Ongoing root D2c acceptance may create legitimate
new facts/runs. A future capture must read fresh obligations and preserve exact
current rows rather than demand old fixture counts.

The updated source passed122 focused recovery/restore Python tests. A separate
actual read-only process pass verified all four PID/start/CWD/loaded-inode/binary
hashes and recorded sessions, native49705's exact seven bundle entries, and all
four original launcher hashes plus their single-literal prospective import
rebase. It made no DB/provider call, process action or preparation/registry write.
The capture-tail fixture's synthetic native receipt was updated to the current
receipt shape; its installed volume/image pipeline was not rerun for this
selection-only change. The successful historical G4 mechanism gates remain
attributed to their original frozen sources.

The original recipe hash was preserved. Its separate
`ROOT_ROLLOUT_RECIPE_HEALTH_CORRECTION.md` notes that relay8089 uses `/_readiness`
and `/_liveness`; Ortak API has no dedicated health route. Protected API401 plus
actual signed native/API200 establishes the recorded application read evidence.
The root's wrong-URL read-only validator failure was retained as such.

No73 full-stack capture or owner registry was created in this source update.
The successful G4 capture/offline restore remain exact schema69/native6ff results;
their20 frozen operators, helpers and historical receipts remain unchanged.

## Original storage-support checkpoint (before deployment)

The following schema/storage rehearsal evidence predates actual73 promotion.
It added recovery support without deploying a service at that time.

| Migration | Retained evidence and ownership | Recovery consequence |
| --- | --- | --- |
|70 | `work_decomposition`, company-owned, key `(company_id, child_id)` | Keep immutable parent/child/version/actor/operation evidence. It is not a community-owned purge table or a dispatch queue. A proposed child does not itself block capture. |
|71 | `run_reviewed_memory_uses`, company/community-owned, key `(company_id, run_id, ordinal)` | Keep exact use pins and complete `run_context_snapshots.spec_bytes`. Existing target `runtime_consumption_enabled` and `consumption_epoch` remain full-row evidence. Missing/nonterminal parent runs block capture. |
|72 | Functions only | The injective JSON comparison transform never changes archived snapshot bytes. NUL/SOH/literal escapes must survive the ordinary bytea dump/restore unchanged. |
|73 | Functions/triggers only | Keep canonical DM participant identity, channel visibility, TTL fields and retained run evidence. Restore never resets TTL, advances an Office generation or renews admission. |

Terminal use evidence remains valid historical evidence after fact withdrawal,
target opt-out, community purge or expiry. Recovery does not call current
eligibility functions to discard it. Existing active-run, cancellation, outbox,
Office/memory/Work output, management, probe and export drains remain required.
Future unattempted withdrawal jobs are retained obligations under the existing69
rules; backup never creates a remote ACK or performs withdrawal. Offline
verification compares exact keys/full-row hashes with activation disabled even
if time makes a saved future job due.

`private_recovery_obligations.py` now validates a complete successful1–N SQLx
ledger with strictly contiguous integer versions and96-character lowercase
checksum encodings. Preparation still binds the actual checksums/schema hash;
syntax alone is not a source pin. Reviewed versions are61–73. Exact per-version
table classification rejects missing tables, alias collisions, tables presented
before their migration and all six source-only workspace74 tables. Version74
and later remain refused. The schema69 contract shape and drain semantics remain
unchanged. The root pause preflight records the actual reviewed schema version
instead of a literal69.

## Evidence

Final focused Python gates passed134 tests with one existing opt-in DB test
skipped (135 total). An independent source review found no actionable issue in
the retained-history/active-run distinction or74/semantic exclusion.

The unchanged69 populated SQL rehearsal passed25 checks against a new retained
55432 database:
`/private/tmp/ortak-v0-evidence/g-obligations69-5cfd558c21f9485ebb7a81de398b4941/receipt.json`.

The final73 rehearsal passed at
`/private/tmp/ortak-v0-evidence/g-schema73-25962833c0f84cf185b6a0fbb1cf1cb7/receipt.json`.
It used the root-selected immutable73 DB bootstrap and four frozen production
local tests: decomposition canonical purge, populated reviewed-use canonical
purge, JSON/NUL runtime input, and private DM participant/archived reads.
Runtime/memory boundaries were controlled adapters; no provider was used.
Production G observation accepted the retained company decomposition and a
terminal revoked use, and refused an actual active use. An expired-use fault
was confined to a replica-mode transaction and rolled back; positive fixture
creation used all production constraints and triggers.

The fixture then created an actual custom-format PG archive and restored it to
another fresh retained55432 database using the existing exact migration45
compatibility helper. All129 tables, complete raw catalog, logical row hashes,
settings and sequence values matched. This includes raw snapshot bytes and
retained DM rows. The full operation has a900-second deadline, bounded child
output and SQL timeouts, a32MiB archive bound, explicit loopback55432 selection
and reconstructed child environments. No existing database, live source,
credential, Docker service or restored executor was used. This proves the
schema73 storage path, not a live full-stack capture or independent-host DR.

The first fixture attempt selected an incorrect exact test module path. The
zero-tests check refused it; failed database/evidence
`g-schema73-36e8f6cd767c4a24b2fd42270be0abfb` remain retained. Later runs used the
verified test listing and passed. The final receipt above binds the final source
hashes and immutable production binaries.

Root separately verified the untouched baseline44 artifact upgrading populated
synthetic Office data through immutable73 and reapplying73:
`/private/tmp/ortak-v0-evidence/baseline44-upgrade73-38adceeae8864aaab6e72b3888b3ab39/receipt.json`.
That evidence preserves old rows, old ledger entries, settings and sequences;
it does not involve the live application or a provider.

## Before selecting a real73 recovery operation

1. Root deployed the reviewed backend73/native and Honcho D2c artifacts above.
   Before a new operation, revalidate actual PID/start/cwd/loaded inode/hash and launcher/session
   provenance for relay, API, worker, management and native ingress. The new
  73 schema hash, full ledger, actual image IDs/mounts and public configuration
   must come from that selection, not from this fixture or the staged build.
2. Current73 exact selection points are updated in `private_recovery_inventory.py`,
   `recovery_native_ingress.py` and the launcher/receipt selection in
   `register_private_recovery.py`, with configuration checks in
   `prepare_private_recovery.py`. Future changed generations require explicit
   new selections. D2c adds no Honcho table or
   background writer: all four D2a reviewed tables and the same API lifecycle
   drain remain, with a newly bound immutable API image.
3. Create a new preparation/owner registry/operator closure. Freeze the updated
   pause helper under a new path/hash and bind its matching validation loader;
   do not overwrite the retained G4 helper. The old16-row completed-Work/fact
   baseline in `record_private_recovery_resume.py` is acceptance-fixture-specific.
   A new operation needs an explicitly frozen current persistence baseline and
   actual new root session receipts; ordinary legitimate data changes must not
   be mistaken for drift against an unrelated historical fixture.
4. Revalidate all drain generations immediately before root's coordinated pause.
   Preserve whole raw schema/row/settings/sequence checks and existing archive,
   MinIO xattr, journal-WAL and seal-failure protections. A73 archive cannot be
   authorized by an old69 approval or by a previous observed drain.

## Separately reviewed future execution surfaces

Second/third employees need a separately reviewed explicit public/secret scope:
fresh signer leaf, immutable selection and three-file profile, enrollment recipe,
each employee's own OAuth identity/state/lock, memory intent/native receipts and
both exported consumer configs. Include original intent-associated staged leaves
only through reviewed exact recovery rules; an unknown pending file must not be
globbed into authority. Preserve Ada's variants, catalog and signer; no relabeling
or key/OAuth copy. Whole-company PG/journal/Honcho backups retain corresponding
new rows automatically, but their file roots, controller binds, signer resolver
and all refresh owners require new explicit selection before preparation.

Semantic scoring has no new PG relation, but it is a separate listener and OAuth
maintenance writer. A future registry must contain its exact process/image,
configuration, private bearer reference and selected OAuth store. `active_scores=0`
does not establish drain: admission, score tasks and the credential owner must
stop, with the reviewed45-second minimum grace and existing OAuth lock/state
preservation. It is not automatically included in schema73 support.

C2/74 remains outside the reviewed version set. Its proposed retained PG tables
are `workspace_bindings`, `workspace_files`, `run_workspace_uses`,
`workspace_tool_actions`, `workspace_tool_receipts` and
`workspace_reader_executions`. The last table retains prepare/read child identity,
planned/running/stopped state and actual containment proof; an expired lease is
not a stop witness. A bounded existing-worker
lane and a one-shot `ortak-workspace-reader` child require explicit selected
input/run roots, immutable reader provenance and exact deadline/kill/reap
containment. Future PG pending/result-ready actions and SQLite
`workspace_tool_calls` pending/resolved rows must settle, including terminal
lost-ACK cleanup. Whole-journal backup must retain the paired `workspace_runs`
metadata and hashes after result text erasure. Workspace capability stays
default-off and requires a separately validated worker-image digest. No74
readiness claim or extra execution authority follows from this G73 checkpoint.
